#include <iostream>
#include <string>
#include <fstream>
#include <assert.h>
#include "../utils.h"

/// @brief  Prints the answer to task 1
/// @param filename name of the input file
void day1(std::string &filename);

/// @brief Prints the answer to task 2 on std::out
/// @param filename name of the input file
void day2(std::string &filename);